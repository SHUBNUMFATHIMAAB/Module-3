# Tuple in Python: Check Element Existence

## 🎯 Aim
To write a Python program that checks if the element `'n'` and the element `8` exist within a given tuple.

## 🧠 Algorithm
1. Define a tuple `x` with some letters and numbers.
2. Use the `in` operator to check if the string `'n'` exists within the tuple.
3. Use the `in` operator to check if the integer `8` exists within the tuple.
4. Print the results.

## 🧾 Program
<img width="489" height="155" alt="image" src="https://github.com/user-attachments/assets/2d17faad-fc9a-4736-9a57-3934f1fb9037" />

## Output
<img width="414" height="190" alt="image" src="https://github.com/user-attachments/assets/875eae6e-0e48-4ae6-b626-e4e641bca67c" />

## Result
Thus the Python program that checks if the element `'n'` and the element `8` exist within a given tuple is created.
